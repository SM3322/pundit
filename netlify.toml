[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Auto-score every hour
[functions."score-matches"]
  schedule = "0 * * * *"

# Send notifications every hour (30 min offset)
[functions."send-notifications"]
  schedule = "30 * * * *"
